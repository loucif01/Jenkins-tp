plugins {
    id 'java'
    id 'jacoco'
}
group 'matrix'
version '1.0'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

task javadocs(type: Javadoc) {
    source = sourceSets.main.allJava
}

uploadArchives {
    repositories {
        maven {
            credentials {
                username "myMavenRepo"
                password "esi2018"
            }
            url "https://mymavenrepo.com/repo/0F2OGmMan2RbvDDdPKza/"
        }
    }
}

